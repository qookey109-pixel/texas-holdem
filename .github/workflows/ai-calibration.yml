name: AI calibration

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "app.js"
      - "js/ai-*.js"
      - "js/boss-*.js"
      - "js/fair-special-bosses.js"
      - "tests/support/ai-calibration-*.js"
      - "tests/support/ai-gameplay-calibration-*.js"
      - "tests/e2e/ai-calibration-*.spec.js"
      - "tests/e2e/ai-gameplay-calibration-*.spec.js"
      - "docs/ai-calibration-*.md"
      - "docs/ai-gameplay-calibration-*.md"
      - "docs/calibration-results/*.md"
      - "package.json"
      - ".github/workflows/ai-calibration.yml"
      - ".github/workflows/ai-long-run-telemetry.yml"
      - ".github/ai-long-run-triggers/**"
      - ".github/ai-long-run-stability-triggers/**"

permissions:
  contents: read

jobs:
  calibration:
    name: fixed-seed calibration
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "24"
          cache: npm

      - name: Install locked dependencies
        run: npm ci

      - name: Install Chromium
        run: npx playwright install --with-deps chromium

      - name: Run AI V1.6, V1.9 and V2.7 calibration
        run: npm run test:ai-calibration

      - name: Upload calibration report
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: ai-calibration-report-${{ github.run_id }}
          path: |
            playwright-report/
            test-results/
          if-no-files-found: warn
          retention-days: 14
