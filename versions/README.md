# 德州撲克版本快照

這個資料夾用來保存最近三次大改前或穩定後的版本。

固定命名格式：

```text
v版本號-改動註解-日期
```

例如：

```text
v41-winner-glow-2026-07-06
v44-hero-card-slim-2026-07-06
```

`改動註解` 要用簡短描述，讓檔案列表一眼看得出這版做了什麼。

每個版本資料夾至少保存：

```text
index.html
styles.css
app.js
VERSION.md
```

目前只保留最近三版：

```text
v75-smaller-table-2026-07-18
v76-ux-readability-tuning-2026-07-18
v77-pixel-card-theme-2026-07-19
```

若要回到某一版，將該版本資料夾內的 `index.html`、`styles.css`、`app.js` 還原到專案根目錄即可。

最新穩定版：`v77-pixel-card-theme-2026-07-19`

重點：

- 玩家桌面籌碼數字上移，降低與玩家手牌重疊的機率。
- 版面儲存 key 升級到 `texasHoldemTableLayoutV3`，重新套用新的安全預設座標。
- 主要操作按鈕亮度提高，`跟注/過牌` 與 `加注` 更像可點擊狀態。
- 已棄牌玩家與蓋牌區更淡，視覺上更快退出焦點。
- 當前行動玩家加入更明顯的呼吸發光。
- 牌局紀錄字級、行距與 padding 微調，提升可讀性。
- 牌桌木框與綠色桌布整體縮小一圈，讓玩家手牌、資訊欄與操作按鈕不再貼得太緊。
- 桌面底部留白增加，減少操作按鈕與玩家手牌互相擋住的機率。
- 手機橫向斷點也同步縮小牌桌比例。
- 操作按鈕移入牌桌內，變成玩家資訊右側附近的輕量浮動操作條。
- 移除操作列的大外框，降低遮擋感與視覺重量。
- 快捷下注移除重複的 `All-in`，保留主動作按鈕的 All-in。
- 操作按鈕加入版面編輯器，可拖曳與方向鍵微調。
- 上方工具按鈕取消分組，回到單排扁平工具列。
- 牌桌左上角莊家/盲注資訊取消外框，改成較輕的純文字狀態。
- 玩家手牌移除左右傾斜角度，避免看起來像版面跑掉。
- 放大最上方「第 N 局」徽章，讓目前局數比牌桌角落重複文字更清楚。
- 牌桌左上角維持兩列狀態：莊家、盲注，但不再使用框線卡片。
- 移除牌桌左上角重複的局數資訊，局數只保留在最上方標題旁。
- 牌桌左上角改成兩列狀態：莊家、盲注。
- 結算報告改為更緊湊的一屏復盤。
- 上方新增完成手數、入池率、最大底池摘要。
- 技術分析與本輪成就合併，避免資訊被裁在視窗下方。
- 技術分析改成兩欄小卡，閱讀更快。
- AI 對話新增更生活化、帶梗的桌邊台詞。
- 新增 AI 情緒狀態：新來、冷靜、自信、上頭、龜縮。
- AI 連贏會變得更自信；連輸會變上頭或龜縮。
- 情緒會小幅影響 AI 攻擊、詐唬與耐心，不會完全蓋掉原本角色個性。
- 座位資訊與 AI 角色資訊卡會顯示目前情緒。
- 新增情緒觸發對話，讓角色反應更生活化。
- Owl 籌碼歸零後顯示本輪結算。
- 結算表包含七邊形玩家風格輪廓、行為統整圖表、成就與技術分析。
- 七邊形顯示耐心、壓迫、好奇、膽量、控池、攤牌、收割；原始行為數據改用長條圖。
- 結算視窗壓縮為一屏內閱讀，移除風格膠囊與自己的資訊欄風格類型顯示。
- 結算內容改為 2x2 資訊版面，七邊形、行為圖表、成就、技術分析同屏顯示。
- 少於 5 手時結算會提示樣本不足，不會太早把玩家風格定型。
- AI 對話加量並更生活化，冷卻與每街上限微調。
- AI 對話加入更多梗感台詞，角色個性更明顯。
- 牌面改成乾淨像素牌組風格，數字牌更清楚，J/Q/K 改為簡化像素人物。
- 牌背改成幾何紋樣，明亮模式與暗色模式共用一致的牌組語言。
- 關閉結算表後重新從第 1 局開始。
- 新 AI 買入改為較平滑的 `3000 / 4500 / 7000 / 10000 / 15000`。

目前唯一正式同步位置：

```text
/Users/qoo/Documents/GitHub/texas-holdem
```

`/Users/qoo/Desktop/德州` 不是正式開發來源。Repository 內的 `docs/` 歷史副本已於 2026-07-22 移除，需要時可從 Git history 還原。
